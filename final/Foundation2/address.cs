public class Address
{
    private string _streetAddress;
    private string _city;
    private string _stateProvince;
    private string _country;

    public inUSA()
    {
        return bool
    }
    public combined(string streetAddress, string city, string stateProvince, string country)
    {
        string combinedInfo = $"{_address} / {_city} / {_stateProvince} / {_country}";
        return combinedInfo;
    }
}